type ArchiveCardType = {
  archive: any;
};

const ArchiveCard = ({ archive }: ArchiveCardType) => {
  return <div className="w-[250px] h-[250px] bg-gray-500 rounded"></div>;
};

export default ArchiveCard;
